function compareFields(...fields) {
  // Process each field in the array
  for (let field of fields) {
    // Do something with the field
    console.log(field);
  }
}

// Example usage
compareFields('field1', 'field2', 'field3');
